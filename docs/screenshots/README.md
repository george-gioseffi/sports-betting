# Screenshots

Captured at 1440p, light theme, 100% zoom, PNG format.

Use `Portugues` interface mode for consistency with existing screenshots.

After running the pipeline, open the app and capture each page at the default filter state (all strategies, leagues, and markets selected).
